{
  "name": "quiver",
  "private": true,
  "version": "1.0.0",
  "description": "A motion microscope in your browser. Amplifies the invisible: your heartbeat, your breathing, the vibration of anything. On-device, zero uploads.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
